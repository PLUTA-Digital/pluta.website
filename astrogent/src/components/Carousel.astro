---
import '@glidejs/glide/dist/css/glide.core.min.css';
import '@glidejs/glide/dist/css/glide.theme.min.css';

// Screenshots - erweitere diese Liste bei Bedarf
const screenshots = ['screen1.jpg', 'screen2.jpg'];
---

<section class="bg-white px-4 py-16 sm:px-6 lg:px-8">
  <div class="mx-auto max-w-7xl">
    <div class="glide">
      <div class="glide__track" data-glide-el="track">
        <ul class="glide__slides">
          {screenshots.map((screenshot, index) => (
            <li class="glide__slide">
              <div class="flex items-center justify-center">
                <img
                  src={`/screenshots/${screenshot}`}
                  alt={`Screenshot ${index + 1}: PLUTA LiquiTool`}
                  loading="lazy"
                  class="max-h-[600px] w-auto rounded-lg shadow-xl"
                />
              </div>
            </li>
          ))}
        </ul>
      </div>

      <div class="glide__arrows" data-glide-el="controls">
        <button class="glide__arrow glide__arrow--left bg-black text-white rounded-full p-3" data-glide-dir="<">
          ‹
        </button>
        <button class="glide__arrow glide__arrow--right bg-black text-white rounded-full p-3" data-glide-dir=">">
          ›
        </button>
      </div>

      <div class="glide__bullets" data-glide-el="controls[nav]">
        {screenshots.map((_, index) => (
          <button class="glide__bullet bg-white border-2 border-black rounded-full w-3 h-3" data-glide-dir={`=${index}`}></button>
        ))}
      </div>
    </div>
  </div>
</section>

<script>
  import Glide from '@glidejs/glide';

  new Glide('.glide', {
    type: 'carousel',
    autoplay: 5000,
    hoverpause: true,
    rewind: true,
  }).mount();
</script>

<style>
  .glide__bullet--active {
    background-color: #000 !important;
    border-color: #000 !important;
    width: 1.5rem !important;
  }
</style>
